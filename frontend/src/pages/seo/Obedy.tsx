import { LandingPage } from "./LandingPage";
import { OBEDY_PAGE } from "../../data/landingPages";

/** /obedy — SEO-лендинг «Домашние обеды с доставкой / готовая еда». */
export function Obedy() {
  return <LandingPage data={OBEDY_PAGE} />;
}
